import type { Route, Settings } from '@/@types'

const defineRouter = (routes: Route[]): Route[] => routes
const defineSetting = (settings: Settings): Settings => settings

export { defineRouter, defineSetting }
